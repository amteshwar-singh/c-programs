//array is a collection of similar elements
// it allows a single variable to store multiple values
#include<stdio.h>

int main() {
    int marks[90];
    marks[0]=45;
    marks[1]=48;
    // we can go all the way till marks 89
    printf("Marks 0 and marks 1 is %d %d",marks[0],marks[1]);

   return 0;
}