#include<stdio.h>

int main() {
     //to check if the number is divisible by 97
     // if remainder is zero than its divisible otherwise not
     int a;
     printf("Enter the number");
     scanf("%d",&a);
     printf("The remainder is %d",a%97);
    return 0;
}