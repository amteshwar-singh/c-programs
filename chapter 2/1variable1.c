#include<stdio.h>

int main() {
    int a = 10;//Declare intialize i with 10
    int b=a;
    //%d,%f,%c is called format specifier
    printf("The value of i is %d and value of j is %d",a,b);
    return 0;
}